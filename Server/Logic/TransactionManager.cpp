#include "TransactionManager.h"
void TransactionManager::execute(const Transaction& transact,
                                 std::unique_ptr<exc::Exception>& e,
                                 unsigned long long transact_num,
                                 std::ostream& out) {}
